<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Extensión de vista de producto -->
    <record id="product_template_form_view" model="ir.ui.view">
        <field name="name">product.template.form.inherit.cdv</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">

            <xpath expr="//group[@name='group_standard_price']" position="after">
                <group string="Información adicional" name="cdv_production">
                    <field name="cdv_is_raw_material"/>
                    <field name="cdv_is_finished_product"/>
                </group>
            </xpath>

            <!-- Añadir UoM preferidas después del grupo de información adicional -->
            <xpath expr="//group[@name='cdv_production']" position="after">
                <group string="Unidades de Medida Preferidas" name="uom_preferred">
                    <field name="uom_production_preferred_id"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="uom_purchase_preferred_id"
                           options="{'no_create': True, 'no_open': True}"/>
                </group>
            </xpath>
        </field>
    </record>

    <!-- Extensión de vista de búsqueda para agregar filtros -->
    <record id="product_template_search_view_cdv" model="ir.ui.view">
        <field name="name">product.template.search.inherit.cdv</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_search_view"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='filter_to_sell']" position="after">
                <separator/>
                <filter string="Materias Primas"
                        name="filter_raw_material"
                        domain="[('cdv_is_raw_material', '=', True)]"/>
                <filter string="Productos Elaborados"
                        name="filter_finished_product"
                        domain="[('cdv_is_finished_product', '=', True)]"/>
            </xpath>
        </field>
    </record>

</odoo>

