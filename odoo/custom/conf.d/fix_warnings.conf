[options]
; Fijar smtp_user y smtp_password como cadenas vacías para evitar el warning
; "isn't a boolean option" que aparece cuando doodba los pone como 'false'
smtp_user =
smtp_password =

; Declarar http_interface explícitamente para evitar el warning de deprecación en 20.0
http_interface = 0.0.0.0
