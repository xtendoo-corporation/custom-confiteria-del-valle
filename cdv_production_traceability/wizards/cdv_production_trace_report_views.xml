<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Vista Tree de las líneas de trazabilidad -->
    <record id="view_cdv_production_trace_report_line_tree" model="ir.ui.view">
        <field name="name">cdv.production.trace.report.line.tree</field>
        <field name="model">cdv.production.trace.report.line</field>
        <field name="arch" type="xml">
            <list decoration-danger="trace_status == 'missing'" decoration-success="trace_status == 'found'" create="false" edit="false" delete="false">
                <field name="production_date" string="Fecha"/>
                <field name="component_product_id" string="Materia Prima"/>
                <field name="component_lot_id" string="Lote Materia Prima"/>
                <field name="trace_status" widget="badge" string="Estado"/>
            </list>
        </field>
    </record>

    <!-- Vista Form del wizard de trazabilidad -->
    <record id="view_cdv_production_trace_report_form" model="ir.ui.view">
        <field name="name">cdv.production.trace.report.form</field>
        <field name="model">cdv.production.trace.report</field>
        <field name="arch" type="xml">
            <form string="Informe de Producto Elaborado">
                <sheet>
                    <div class="alert alert-warning" role="alert" invisible="not warning_message">
                        <field name="warning_message" nolabel="1"/>
                    </div>
                    <group>
                        <group string="Filtros">
                            <field name="company_id" invisible="1"/>
                            <field name="date_from"/>
                            <field name="date_to"/>
                            <field name="product_id" options="{'no_create': True}"/>
                            <field name="lot_id" options="{'no_create': True}"/>
                        </group>
                        <group string="Resumen" invisible="not line_ids">
                            <field name="total_productions" readonly="1"/>
                            <field name="total_qty_produced" readonly="1"/>
                            <field name="total_components" readonly="1"/>
                        </group>
                    </group>
                    <notebook invisible="not line_ids">
                        <page string="Detalle de Materias Primas" name="trace_lines">
                            <field name="line_ids" nolabel="1"/>
                        </page>
                    </notebook>
                </sheet>
                <footer>
                    <button string="Calcular trazabilidad" name="action_compute_trace" type="object"
                            class="btn-primary" invisible="line_ids"/>
                    <button string="Imprimir informe" name="action_print_report" type="object"
                            class="btn-primary" invisible="not line_ids"/>
                    <button string="Nueva búsqueda" name="action_reset" type="object"
                            class="btn-secondary" invisible="not line_ids"/>
                    <button string="Cancelar" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Acción del wizard -->
    <record id="action_cdv_production_trace_report" model="ir.actions.act_window">
        <field name="name">Informe de Producto Elaborado</field>
        <field name="res_model">cdv.production.trace.report</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{}</field>
    </record>

</odoo>

